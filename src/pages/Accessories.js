import access from '../assets/access.png';

function Accessories(props) {
    return(
        <div className="products">
            <img src={access} className="products" alt="products" />
        </div>

    )
};

export default Accessories;