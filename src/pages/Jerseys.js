import jerseys from '../assets/jerseys.png';

function Jerseys(props) {
    return(
        <div className="products">
            <img src={jerseys} className="products" alt="products" />
        </div>

    )
};
export default Jerseys;